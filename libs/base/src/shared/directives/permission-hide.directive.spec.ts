import { PermissionHideDirective } from './permission-hide.directive';

describe('PermissionHideDirective', () => {
  it('should create an instance', () => {
    const directive = new PermissionHideDirective();
    expect(directive).toBeTruthy();
  });
});
