import { UiRadioCompareWithDirective } from './ui-radio-compare-with.directive';

describe('UiRadioCompareWithDirective', () => {
  it('should create an instance', () => {
    const directive = new UiRadioCompareWithDirective();
    expect(directive).toBeTruthy();
  });
});
