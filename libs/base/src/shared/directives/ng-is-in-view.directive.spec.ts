import { NgIsInViewDirective } from './ng-is-in-view.directive';

describe('NgIsInViewDirective', () => {
  it('should create an instance', () => {
    const directive = new NgIsInViewDirective();
    expect(directive).toBeTruthy();
  });
});
